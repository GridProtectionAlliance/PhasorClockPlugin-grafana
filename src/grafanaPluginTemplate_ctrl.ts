import { MetricsPanelCtrl } from 'app/plugins/sdk';

export class GraphanaPluginTemplateCtrl extends MetricsPanelCtrl{
    constructor($scope, $injector, $rootScope) {
        super($scope, $injector);
    }
}